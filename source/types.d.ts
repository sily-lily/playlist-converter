export type cache = {
    savedPlaylists: number,
    musicApps: Array<string>
}

export type settings = {
    textColor: {
        focused: string,
        unfocused: string
    },
    lineColor: {
        focused: string,
        unfocused: string
    }
}