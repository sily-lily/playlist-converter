export type cache = {
    savedPlaylists: Array<any>,
    musicApps: Array<string>
}

export type settings = {
    textColor: {
        focused: string,
        unfocused: string
    },
    lineColor: {
        focused: string,
        unfocused: string
    }
}